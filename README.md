This project is a learning based project used to continually build on present
knowledge of c++ using the SDL2 library.

compilation command (to be added to makefile):

g++ main.cpp -o sdl_demo `sdl2-config --cflags --libs` -lSDL2_image -lSDL2_ttf -lSDL2_mixer
